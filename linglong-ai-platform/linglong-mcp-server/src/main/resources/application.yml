server:
  port: 8083

spring:
  application:
    name: linglong-mcp-server
  
  # MySQL数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/linglong_mcp?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: 20041212
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  # JPA配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

# 日志配置
logging:
  level:
    com.linglong.mcp: DEBUG
